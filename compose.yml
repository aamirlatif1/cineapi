services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cineapi
      POSTGRES_USER: cineapi
      POSTGRES_PASSWORD: cineapi
    ports:
      - "5432:5432"
    volumes:
      - cineapi_postgres_data:/var/lib/postgresql/data

volumes:
  cineapi_postgres_data:
